{
  "name": "trie",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "jest": "^24.8.0",
    "snazzy": "^8.0.0",
    "standard": "^12.0.1"
  },
  "scripts": {
    "lint": "standard | snazzy",
    "fix": "standard --fix",
    "test": "jest",
    "test:watch": "jest --watchAll --coverage"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
